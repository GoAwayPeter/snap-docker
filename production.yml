version: '3'
services:
  snapapp:
    build:
      context: .
      dockerfile: docker/snap.prod
    ports:
      - 8000:8000
    links:
      - db
  db:
    image: postgres:9.6.5
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
